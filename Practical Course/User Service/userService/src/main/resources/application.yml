spring:
  liquibase:
    change-log: classpath:db/changelog-master.xml
  datasource:
    url: jdbc:postgresql://localhost:5432/user_service_db
    username: papova_yana
    password: 1234
  jpa:
    show-sql: true
  cache:
    type: redis
    redis:
      host: localhost
      port: 6379